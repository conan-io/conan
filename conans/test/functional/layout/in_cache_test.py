import textwrap
import os

import unittest

from conans.test.assets.genconanfile import GenConanfile
from conans.test.utils.tools import TestClient


class CacheWithLayoutTest(unittest.TestCase):
    # Tests that show how the folders in the cache respond to the declared layout in the recipe
    pass
